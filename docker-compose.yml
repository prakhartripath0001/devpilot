services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: devpilot-postgres

    environment:
      POSTGRES_DB: devpilot
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

    volumes:
      # PostgreSQL persistent data
      - postgres_data:/var/lib/postgresql/data

      # Initialize PostgreSQL extensions
      - ./docker/postgres/init-extension.sql:/docker-entrypoint-initdb.d/init-extension.sql:ro

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d devpilot"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

#  backend:
#    build:
#      context: ./backend
#      dockerfile: Dockerfile
#    container_name: devpilot-backend
#    ports:
#      - "8080:8080"
#    environment:
#      - DB_URL=jdbc:postgresql://postgres:5432/devpilot
#      - DB_USERNAME=postgres
#      - DB_PASSWORD=postgres
#      - OPENAI_API_KEY=${OPENAI_API_KEY}
#      - ENCRYPTOR_PASSWORD=${ENCRYPTOR_PASSWORD}
#      - ENCRYPTOR_SALT=${ENCRYPTOR_SALT}
#      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
#      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET}
#    depends_on:
#      postgres:
#        condition: service_healthy

#  client:
#    build:
#      context: ./client
#      dockerfile: Dockerfile
#    container_name: devpilot-client
#    ports:
#      - "3000:3000"
#    environment:
#      - NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
#      - NEXT_PUBLIC_GITHUB_CLIENT_ID=${NEXT_PUBLIC_GITHUB_CLIENT_ID}
#    depends_on:
#      - backend

volumes:
  postgres_data: